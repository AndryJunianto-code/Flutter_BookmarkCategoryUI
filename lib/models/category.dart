import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class Category {
  String title;
  IconData logo;
  final Color bgColor;
  final Color fontColor;
  Category(this.title, this.logo, this.bgColor, this.fontColor);
}
